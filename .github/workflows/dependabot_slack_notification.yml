name: Dependabot Slack Notification (Scheduled)

on:
  workflow_dispatch:
    inputs:
      severity_filter:
        description: "Severity levels to notify (all / critical,high / medium / low)"
        required: false
        default: "all"
      mention:
        description: "Slack mention target (e.g. <!channel>, <!here>, <@U12345>)"
        required: false
        default: ""

jobs:
  # ---------- Scheduled runs ----------
  daily-critical-high:
    if: github.event_name == 'schedule' && github.event.schedule == '0 0 * * 1-5'
    uses: ./.github/workflows/_dependabot_slack_notification.yml
    with:
      severity_filter: "critical,high"
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      DEPENDABOT_TOKEN: ${{ secrets.DEPENDABOT_TOKEN }}

  weekly-medium:
    if: github.event_name == 'schedule' && github.event.schedule == '0 0 * * 1'
    uses: ./.github/workflows/_dependabot_slack_notification.yml
    with:
      severity_filter: "medium"
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      DEPENDABOT_TOKEN: ${{ secrets.DEPENDABOT_TOKEN }}

  monthly-low:
    if: github.event_name == 'schedule' && github.event.schedule == '0 0 1 * *'
    uses: ./.github/workflows/_dependabot_slack_notification.yml
    with:
      severity_filter: "low"
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      DEPENDABOT_TOKEN: ${{ secrets.DEPENDABOT_TOKEN }}

  # ---------- Manual run ----------
  manual:
    if: github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/_dependabot_slack_notification.yml
    with:
      severity_filter: ${{ inputs.severity_filter }}
      mention: ${{ inputs.mention }}
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      DEPENDABOT_TOKEN: ${{ secrets.DEPENDABOT_TOKEN }}
